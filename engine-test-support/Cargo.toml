[package]
name = "engine-test-support"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.8.9", features = ["macros"] }
monero = "0.22.0"
moneropay-core = { path = ".." }
tokio = { version = "1.53.1", features = ["full"] }

[dev-dependencies]
reqwest = { version = "0.13.4", default-features = false, features = ["rustls", "json"] }
