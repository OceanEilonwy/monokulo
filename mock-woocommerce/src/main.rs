//! `mock-woocommerce`: a fake WooCommerce store used to exercise the engine
//! and control-plane end-to-end without a real WordPress/WooCommerce
//! install. Empty skeleton for now — see `docs/WOOCOMMERCE_WBS.md` §5,
//! Stage 5.

fn main() {}

#[cfg(test)]
mod tests {
    #[test]
    fn compiles() {
        assert!(true);
    }
}
